@begin(section) @title(MNAS-STRING/DB)

Пакет @b(mnas-string) экспортирует следующие функции:
 @begin(list) 
  @item(@b(prepare-to-query) - подготовка строки в качестве аргумента для like запроса SQL.)
 @end(list)
@begin(section)
 @title(Функции)
 @cl:with-package[name="MNAS-STRING/DB"](
  @cl:doc(function prepare-to-query))
@end(section)
@end(section)
